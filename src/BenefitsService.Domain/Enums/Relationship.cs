
namespace BenefitsService.Domain.Enums
{
    /// <summary>
    /// Relationship enum representing who a dependent is in relation to an employee.
    /// </summary>
    public enum Relationship
    {
        Spouse,
        DomesticPartner,
        Child,
        Parent
    }
}
